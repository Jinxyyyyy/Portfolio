# Program to compute the circumference of a circle
import math

def circleCirconference():
    pi = math.pi
    radius = input(f"Enter the circle's radius:")
    circumference = 2 * pi * int(radius)
    print(f"The circumference is {circumference}")

circleCirconference()
